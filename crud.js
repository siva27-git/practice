//CRUD

// C --> CREATE (POST)
// R --> READ (GET)
// U --> UPDATE (PUT, PATCH)
// D --> DELETE (DELETE)

// Post --> used to create new data,
// Get --> use get the data, not be able to pass body payload
// Put --> will send te data with the identifier, if identifer exists it will update else create new record
           // Need to pass complete payload
// PATCH --> To update the data, only updating payload is enough.
// Delete --> to delete a data 